void main() {
  // List<int> numbers = List.generate(1000000, (index) => index);

  // int result = -1;
  // for (var i = 0; i < numbers.length; i++) {
  //   if (numbers[i] == 500000) {
  //     result = numbers[i];
  //     break;
  //   }
  // }

  // print(result);
}
